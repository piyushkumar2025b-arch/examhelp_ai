# ExamHelp AI — .env Template
# NEVER commit .env to git — this file is a template only
# ALL INTEGRATIONS BELOW ARE REMOVED/DISABLED

# ── AI Keys (add NEW keys after revoking exposed ones) ────
# GROQ_API_KEY_1=YOUR_NEW_GROQ_KEY_HERE
# GEMINI_API_KEY_1=YOUR_NEW_GEMINI_KEY_HERE

# ── Supabase (REMOVED) ────────────────────────────────────
# Integration fully disabled. Do not re-enable.

# ── Google OAuth/Maps (REMOVED) ──────────────────────────
# Integration fully disabled. Do not re-enable.

# ── Stripe (REMOVED) ─────────────────────────────────────
# Integration fully disabled. Do not re-enable.

# ── Cloudflare (REMOVED) ─────────────────────────────────
# Integration fully disabled. Do not re-enable.
